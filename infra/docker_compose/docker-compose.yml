name: teamcity-tests-docker-compose

services:
  webapp:
    image: jetbrains/teamcity-server:latest
    container_name: teamcity-server-instance
    ports:
      - "8111:8111"
    volumes:
      - teamcity-data:/data/teamcity_server/datadir
      - teamcity-logs:/opt/teamcity/logs
    healthcheck:
      test: ["CMD-SHELL", "curl -sfL http://localhost:8111/mnt -o /dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 24
      start_period: 30s
    networks:
      - teamcity-net

  agent:
    image: jetbrains/teamcity-minimal-agent:latest
    container_name: teamcity-agent-instance
    environment:
      - SERVER_URL=http://teamcity-server-instance:8111
    volumes:
      - teamcity-agent-conf:/data/teamcity_agent/conf
    depends_on:
      - webapp
    networks:
      - teamcity-net

  setup:
    image: glskill/teamcity-tests-version1:latest
    container_name: teamcity-setup
    depends_on:
      webapp:
        condition: service_healthy
    environment:
      - UI_BASE_URL=http://webapp:8111
      - ENV_FILE=/app/.env
      - TC_LOG_FILE=/teamcity-logs/teamcity-server.log
      - PYTHONPATH=/app
    volumes:
      - ../../:/app
      - teamcity-logs:/teamcity-logs:ro
    entrypoint:
      - sh
      - -c
      - |
        xvfb-run -a python /app/infra/docker_compose/set_up.py
        echo '[setup] Готово, держим контейнер живым...'
        tail -f /dev/null
    healthcheck:
      test: ["CMD-SHELL", "test -f /app/.env"]
      interval: 5s
      timeout: 3s
      retries: 60
      start_period: 10s
    networks:
      - teamcity-net

networks:
  teamcity-net:
    name: teamcity-net

volumes:
  teamcity-data:
  teamcity-logs:
  teamcity-agent-conf: